// material-ui
import { Typography } from '@mui/material';
import MainCard from 'components/MainCard';

const SamplePage = () => {
  return (<MainCard title="About">
    <Typography variant="body2">
      About BaseLine and React!
    </Typography>
  </MainCard>
)};

export default SamplePage;
